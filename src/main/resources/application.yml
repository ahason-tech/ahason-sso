server:
  port: 9000

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ahason_sso
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
  thymeleaf:
    cache: false
